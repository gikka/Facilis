@model IEnumerable<Facilis.MVC.ViewModels.ArquivoViewModel>

<p>
    @Html.ActionLink("Create New", "Create", "Arquivos")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContentType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EventoId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContentType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EventoId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", "Arquivos",new { id=item.ArquivoId }) |
            @Html.ActionLink("Details", "Details", "Arquivos", new { id=item.ArquivoId }) |
            @Html.ActionLink("Delete", "Delete", "Arquivos", new { id=item.ArquivoId })
        </td>
    </tr>
}

</table>
