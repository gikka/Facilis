@model IEnumerable<Facilis.MVC.ViewModels.VideoViewModel>

<table class="table table-hover">
    <tr>
        <th>
            Título do vídeo
        </th>
        <th>
            Url do vídeo
        </th>
        <th style="text-align: center">
            <a href="@Url.Action("Create", "Videos", new { id = ViewData.ContainsKey("id") ? this.ViewData["id"].ToString() : string.Empty })" class="btn btn-primary" data-toggle="tooltip" data-placement="auto" title="Novo arquivo">
                <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
            </a>
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.URL)
        </td>
        <td>
            <a href="@Url.Action("Download", "Videos", new { id = item.VideoId })" , class="btn btn-success" data-toggle="tooltip" data-placement="auto" title="Ver vídeo" target="_blank">
                <span class="glyphicon glyphicon-download" aria-hidden="true"></span>
            </a>
            <a href="@Url.Action("Edit", "Videos",new { id = item.VideoId })" class="btn btn-primary" data-toggle="tooltip" data-placement="auto" title="Alterar">
                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
            </a>
            <a href="@Url.Action("Details", "Videos", new { id = item.VideoId })" , class="btn btn-info" data-toggle="tooltip" data-placement="auto" title="Detalhes">
                <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
            </a>
            <a href="@Url.Action("Delete", "Videos", new { id = item.VideoId })" , class="btn btn-danger" data-toggle="tooltip" data-placement="auto" title="Excluir">
                <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
            </a>
        </td>
    </tr>
}

</table>
